# Copy to .env and adjust for your environment before running `npx expo start`
# Must be an HTTPS origin (no path) so iOS WebView camera works
# Example using ngrok or Cloudflare Tunnel
EXPO_PUBLIC_API_BASE=https://your-tunnel-subdomain.example.dev
